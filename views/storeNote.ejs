<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Add Note</title>
</head>

<body>
  <header>
    <a title="Click to go back to all notes" href="/notes">
      <button>All Notes</button>
    </a>

    <a title="Click to go back" href="/notes">
      <button>Cancel</button>
    </a>
  </header>

  <hr>
  
  <div class="content">
    <h1>
      Add Note
    </h1>

    <% if (locals.error) { %>
      <div class="error-message">
        <strong>
        <%= error %>
      </strong>
      </div>
    <% } %>

    <form class="full-width" action="/notes/create" method="POST">
      <label for="title">Title</label>
      <input type="text" id="title" name="title" autofocus value="<%= locals.note ? note.title : '' %>"/>

      <label for="content">Content</label>
      <textarea name="content" id="content" cols="30" rows="40"><%= locals.note ? note.content : '' %></textarea>

      <button title="Click to save this note" type="submit" id="submitButton">Save Note</button>
    </form>
  </div>
</body>

</html>